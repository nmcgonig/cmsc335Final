<!DOCTYPE html>
<html lang="en"  data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/base.css">
    
</head>
<body class="d-flex flex-column min-vh-100">

    <%- include('navbar') %>

    <main class="container my-5 flex-grow-1">
        
        <h1 class="display-5 fw-bold mb-2">
            <i class="bi bi-compass-fill me-2"></i>Scouting Report: <%= player.username %> vs <%= opponent.username %>
        </h1>
        <p class="lead text-muted mb-4">
            Analysis based on games where <%= player.username %> played as <%= targetColor %>.
        </p>
        <hr class="border-secondary">
        
        <div class="row text-center mb-5">
            <div class="col-md-6">
                <div class="alert alert-primary shadow">
                    <h5 class="mb-0 fw-bold"><%= player.username %> Games Analyzed: <%= player.num %></h5>
                </div>
            </div>
            <div class="col-md-6">
                <div class="alert alert-danger shadow">
                    <h5 class="mb-0 fw-bold"><%= opponent.username %> Openings Analyzed: <%= opponent.num %></h5>
                </div>
            </div>
        </div>
        
        
        <h2 class="mb-3"><i class="bi bi-graph-up me-2"></i>Most Common Openings</h2>
        <div class="row">
            
            <div class="col-md-6 mb-4">
                <div class="card bg-dark border-primary h-100 shadow">
                    <div class="card-header bg-primary text-white fw-bold">
                        <%= player.username %>'s Top 5 Openings
                    </div>
                    <ul class="list-group list-group-flush opening-list">
                        <%- player.mostCommon %>
                    </ul>
                </div>
            </div>

            <div class="col-md-6 mb-4">
                <div class="card bg-dark border-danger h-100 shadow">
                    <div class="card-header bg-danger text-white fw-bold">
                        <%= opponent.username %>'s Top 5 Openings
                    </div>
                    <ul class="list-group list-group-flush opening-list">
                        <%- opponent.mostCommon %>
                    </ul>
                </div>
            </div>
        </div>
        
        <h2 class="mb-3 mt-4"><i class="bi bi-shield-slash-fill me-2"></i>Weakest Openings (Lowest Win Rate)</h2>
        <div class="row">
            
            <div class="col-md-6 mb-4">
                <div class="card bg-dark border-warning h-100 shadow">
                    <div class="card-header bg-warning text-dark fw-bold">
                        <%= player.username %>'s Weakest 5 Openings
                    </div>
                    <ul class="list-group list-group-flush opening-list">
                        <%- player.weakest %>
                    </ul>
                </div>
            </div>

            <div class="col-md-6 mb-4">
                <div class="card bg-dark border-info h-100 shadow">
                    <div class="card-header bg-info text-dark fw-bold">
                        <%= opponent.username %>'s Weakest 5 Openings
                    </div>
                    <ul class="list-group list-group-flush opening-list">
                        <%- opponent.weakest %>
                    </ul>
                </div>
            </div>
        </div>


        <div class="text-center mt-5">
            <a href="/" class="btn btn-success btn-lg"><i class="bi bi-house-door-fill me-2"></i>Start a New Search</a>
        </div>

    </main>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>