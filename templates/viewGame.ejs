<!DOCTYPE html>
<html lang="en" data-bs-theme="dark"> 
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>♟️ Chess Stats Viewer</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/base.css">
</head>
<body>
    
    <%- include('navbar') %>

    <main class="container my-5">
        
        <div class="d-flex justify-content-between align-items-center mb-4 border-bottom pb-3">
            <h1 class="display-5 text-success fw-bold">
                <i class="bi bi-journal-check me-3"></i><%= entry.title %>
            </h1>
            <div>
                <a href="/game/list" class="btn btn-outline-secondary me-2">
                    <i class="bi bi-arrow-left me-1"></i> Back to Library
                </a>
            </div>
        </div>

        <div class="row">
            
            <div class="col-lg-8">
                <div class="card bg-dark text-light shadow-lg mb-4">
                    <div class="card-header bg-success text-dark fw-bold h5">
                        Analysis & Description
                    </div>
                    <div class="card-body">
                        <p class="card-text fs-5">
                                <%= entry.description ||  'No detailed summary was provided for this study entry.' %>
                        </p>
                    </div>
                </div>
            </div>

            <div class="col-lg-4">
                <div class="card bg-secondary text-light shadow-lg">
                    <div class="card-header bg-dark border-bottom border-success fw-bold">
                        Study Details
                    </div>
                    <ul class="list-group list-group-flush">
                        
                        <li class="list-group-item bg-secondary text-light">
                            <strong><i class="bi bi-calendar-event me-2"></i>Date Studied:</strong>
                            <%= new Date(entry.played).toLocaleDateString() %>
                        </li>

                        <li class="list-group-item bg-secondary text-light">
                            <strong><i class="bi bi-code-square me-2"></i>ECO Code:</strong>
                            <%= entry.ecoCode || 'N/A' %>
                        </li>
                        <li class="list-group-item bg-secondary text-light">
                            <strong><i class="bi bi-link-45deg me-2"></i>Source URL:</strong>
                            <a href="<%= entry.URL %>" target="_blank" class="text-info">
                                View Source <i class="bi bi-box-arrow-up-right"></i>
                            </a>
                        </li>

                    </ul>
                </div>
            </div>
        </div>
    </main>
</body>
</html>