<!DOCTYPE html>
<html lang="en"  data-bs-theme="dark"> 
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>♟️ Chess Stats Viewer</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/base.css">
</head>
<body>

    <%- include('navbar') %>

    <div class="container mt-5 pt-3 d-flex flex-column align-items-center">            
            <div class="col-10 mb-4">
                <div class="p-5 text-center bg-secondary rounded-3 shadow-lg">
                    <h1 class="display-4 fw-bold mb-3">
                        Track Your Chess Journey
                    </h1>
                    <p class="lead mb-4 text-light">
                        Log your personal games, view your records, and explore insights powered by the Chess.com API.
                    </p>
                    <form class="d-flex" action="/player/user" method="GET">
                        <div class="input-group">
                            <input class="form-control" type="search" placeholder="Search Chess.com Username" aria-label="Search" name="username" required>
                            <button class="btn btn-success" type="submit">
                                <i class="bi bi-search"> Search</i>
                            </button>
                        </div>
                    </form>
                </div>
            </div>
            
            <div class="col-10 p-5 text-center bg-secondary rounded-3 shadow-lg">
                <h1 class="display-4 fw-bold mb-3">
                    Scout Your Opponent
                </h1>
                <p class="lead mb-4 text-light">
                    Find how your openings align with your opponent
                </p>

                <form class="d-flex flex-column gap-3" action="/player/scout" method="GET">
                    
                    <div class="row g-2 mb-3">
                        <div class="col-md-6">
                            <input class="form-control form-control-lg" type="search" placeholder="Your Chess.com Username" name="player" required>
                        </div>
                        <div class="col-md-6">
                            <input class="form-control form-control-lg" type="search" placeholder="Opponent's Chess.com Username" name="opponent" required>
                        </div>
                    </div>

                    <div class="row justify-content-center">
                        <div class="col-md-8 mb-4">
                            <div class="btn-group w-100" role="group">
                                
                                <input type="radio" class="btn-check" name="color" id="colorWhite" value="WHITE" checked required>
                                <label class="btn btn-outline-light btn-lg" for="colorWhite">
                                    Playing White
                                </label>

                                <input type="radio" class="btn-check" name="color" id="colorBlack" value="BLACK" required>
                                <label class="btn btn-outline-light btn-lg" for="colorBlack">
                                    Playing Black
                                </label>
                            </div>
                        </div>
                    </div>

                    <button class="btn btn-success btn-lg shadow-sm" type="submit">
                        <i class="bi bi-search me-2"></i>Search
                    </button>
                </form>
            </div>
        </div>
</body>
</html>